-- WARNING: This schema is for context only and is not meant to be run.
-- Table order and constraints may not be valid for execution.

CREATE TABLE public.business_profiles (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL UNIQUE,
  legal_name text,
  trade_name text,
  constitution text,
  business_pan text,
  email text,
  phone_number text,
  gstin text,
  portal_username text,
  portal_password text,
  nature_of_business text,
  state text,
  date_of_registration date,
  filing_frequency text,
  annual_turnover_range text,
  registered_address text,
  is_profile_complete boolean DEFAULT false,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  hsn_codes text,
  sac_codes text,
  CONSTRAINT business_profiles_pkey PRIMARY KEY (id),
  CONSTRAINT business_profiles_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.files (
  file_id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  file_url text NOT NULL,
  status text DEFAULT 'pending'::text,
  file_path text,
  created_at timestamp with time zone DEFAULT now(),
  invoice_number text,
  company_name text,
  CONSTRAINT files_pkey PRIMARY KEY (file_id),
  CONSTRAINT files_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(user_id)
);
CREATE TABLE public.gst_invoices (
  gst_invoice_id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  invoice_url text NOT NULL,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT gst_invoices_pkey PRIMARY KEY (gst_invoice_id),
  CONSTRAINT gst_invoices_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(user_id)
);
CREATE TABLE public.pdf (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  company_name text,
  invoice_number text,
  status text,
  reason text,
  links text,
  created_at timestamp with time zone NOT NULL DEFAULT now(),
  CONSTRAINT pdf_pkey PRIMARY KEY (id)
);
CREATE TABLE public.purchase_register (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  source text CHECK (source = ANY (ARRAY['whatsapp'::text, 'email'::text, 'manual'::text, 'bulk'::text])),
  supplier_name text,
  supplier_gstin text,
  supplier_state_code text,
  invoice_number text,
  invoice_date date,
  invoice_type text DEFAULT 'B2B'::text,
  buyer_gstin text,
  place_of_supply_state_code text,
  taxable_value numeric,
  cgst_amount numeric,
  sgst_amount numeric,
  igst_amount numeric,
  cess_amount numeric,
  total_invoice_value numeric,
  hsn_or_sac_code text,
  description_of_goods_services text,
  quantity numeric,
  unit_of_measure text,
  rate_per_unit numeric,
  is_reverse_charge boolean DEFAULT false,
  is_itc_eligible boolean DEFAULT true,
  itc_claimed_cgst numeric,
  itc_claimed_sgst numeric,
  itc_claimed_igst numeric,
  itc_claimed_cess numeric,
  invoice_bucket_url text,
  ocr_raw_json jsonb,
  ocr_confidence_score numeric,
  invoice_status text CHECK (invoice_status = ANY (ARRAY['extracted'::text, 'verified'::text, 'error'::text, 'pending'::text, 'needs_review'::text])),
  created_at timestamp without time zone DEFAULT now(),
  updated_at timestamp without time zone DEFAULT now(),
  CONSTRAINT purchase_register_pkey PRIMARY KEY (id)
);
CREATE TABLE public.purchase_remarks (
  id uuid NOT NULL DEFAULT uuid_generate_v4(),
  purchase_id uuid NOT NULL,
  field_name text NOT NULL,
  issue_type text CHECK (issue_type = ANY (ARRAY['missing'::text, 'unreadable'::text, 'mismatch'::text, 'invalid_format'::text])),
  detected_value text,
  expected_value text,
  confidence_score numeric,
  status text DEFAULT 'open'::text CHECK (status = ANY (ARRAY['open'::text, 'resolved'::text, 'ignored'::text])),
  comment text,
  created_at timestamp without time zone DEFAULT now(),
  CONSTRAINT purchase_remarks_pkey PRIMARY KEY (id),
  CONSTRAINT purchase_remarks_purchase_id_fkey FOREIGN KEY (purchase_id) REFERENCES public.purchase_register(id)
);
CREATE TABLE public.sale_purchase_register (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid NOT NULL,
  sales_register_url text,
  purchase_url text,
  gstr1b_url text,
  reconciliation_json jsonb,
  gstr9b_url text,
  created_at timestamp with time zone DEFAULT now(),
  gstr2b_url text,
  gstr3b_url text,
  CONSTRAINT sale_purchase_register_pkey PRIMARY KEY (id),
  CONSTRAINT sale_purchase_register_user_id_fkey FOREIGN KEY (user_id) REFERENCES public.users(user_id)
);
CREATE TABLE public.sales_invoices (
  id uuid NOT NULL DEFAULT gen_random_uuid(),
  user_id uuid,
  invoice_date date,
  voucher_type text,
  invoice_number text,
  invoice_type text,
  customer_name text,
  customer_gstin text,
  place_of_supply text,
  hsn_sac_code text,
  quantity numeric,
  uqc text,
  rate numeric,
  local_sales_taxable_18 numeric DEFAULT 0,
  local_sales_taxable_12 numeric DEFAULT 0,
  oms_sales_taxable_12 numeric DEFAULT 0,
  taxable_value numeric DEFAULT 0,
  cgst_amount numeric DEFAULT 0,
  sgst_amount numeric DEFAULT 0,
  igst_amount numeric DEFAULT 0,
  tcs_cess numeric DEFAULT 0,
  round_off numeric DEFAULT 0,
  gross_total numeric,
  reverse_charge boolean DEFAULT false,
  eway_bill_number text,
  irn text,
  invoice_file_url text,
  extraction_status text DEFAULT 'pending'::text,
  gemini_raw_json jsonb,
  created_at timestamp with time zone DEFAULT now(),
  updated_at timestamp with time zone DEFAULT now(),
  CONSTRAINT sales_invoices_pkey PRIMARY KEY (id),
  CONSTRAINT sales_invoices_user_id_fkey FOREIGN KEY (user_id) REFERENCES auth.users(id)
);
CREATE TABLE public.users (
  user_id uuid NOT NULL DEFAULT gen_random_uuid(),
  phone_number text NOT NULL UNIQUE,
  created_at timestamp with time zone DEFAULT now(),
  CONSTRAINT users_pkey PRIMARY KEY (user_id)
);